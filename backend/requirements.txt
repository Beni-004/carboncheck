# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.12.5
pydantic-settings==2.11.0

# Database
supabase==2.28.3

# Ground Layer (PDF/HTML parsing)
pdfplumber==0.10.3
beautifulsoup4==4.12.3
lxml==5.1.0

# Satellite Layer - lightweight only
# Note: rasterio/geopandas removed - code has fallback using struct for TIFF parsing
# The Statistical API returns JSON directly, no TIFF parsing needed
tifffile==2024.2.12
shapely==2.0.2

# AI Layer - only scikit-learn is actually used in anomaly_detector.py
# torch, torchvision, xgboost, pyod were never imported - REMOVED to save ~4GB
scikit-learn==1.4.0

# Data Processing
pandas==2.1.4
numpy==1.26.3
pillow==10.2.0

# HTTP Client
httpx==0.28.1
websockets==15.0.1
